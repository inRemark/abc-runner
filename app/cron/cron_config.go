package cron

func init() {
	// Register template functions.
	// c := cron.New(cron.WithSeconds())
	// var err error
	//_, err = c.AddFunc("*/10 * * * * ?", printTest)
	//_, err = c.AddFunc("*/20 * * * * ?", job.TestJob)
	//_, err = c.AddFunc("*/50 * * * * ?", TaskCreate)
	//_, err = c.AddFunc("*/40 * * * * ?", TaskStatus)
	//_, err = c.AddFunc("*/55 * * * * ?", ReportCreate)
	//_, err = c.AddFunc("*/52 * * * * ?", ReportStatus)
	//_, err = c.AddFunc("*/59 * * * * ?", ConvertPdf)
	// if err != nil {
	// 	fmt.Println(err)
	// }
	// c.Start()
	// log.Printf("Cron initialized successfully")
}
